{
  "name": "gbd-dispatch-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "login": "wrangler login",
    "secret:set": "wrangler secret put GITHUB_TOKEN"
  },
  "devDependencies": {
    "wrangler": "^4.84.1"
  }
}
