name = "gbd-dispatch"
main = "src/index.js"
compatibility_date = "2026-01-15"

# GITHUB_TOKEN is a secret — registered via:
#   wrangler secret put GITHUB_TOKEN
# (Do NOT put the token in this file.)
